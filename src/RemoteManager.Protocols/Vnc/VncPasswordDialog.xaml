<Window x:Class="RemoteManager.Protocols.Vnc.VncPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
        Title="VNC Authentication Required"
        Width="420" SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        WindowStyle="ToolWindow"
        Background="{DynamicResource ApplicationBackgroundBrush}">
    <Grid Margin="24">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <TextBlock Grid.Row="0"
                   Text="VNC Server requires a password"
                   FontSize="16" FontWeight="SemiBold"
                   Foreground="{DynamicResource TextFillColorPrimaryBrush}"
                   Margin="0,0,0,4"/>

        <TextBlock Grid.Row="1"
                   x:Name="ServerInfoText"
                   Text="Enter the password to authenticate with the remote VNC server."
                   FontSize="12"
                   Foreground="{DynamicResource TextFillColorSecondaryBrush}"
                   TextWrapping="Wrap"
                   Margin="0,0,0,18"/>

        <!-- Password Field -->
        <StackPanel Grid.Row="2">
            <TextBlock Text="Password" FontWeight="Medium" FontSize="12"
                       Foreground="{DynamicResource TextFillColorPrimaryBrush}" Margin="0,0,0,6"/>
            <ui:PasswordBox x:Name="PasswordInput"
                            PlaceholderText="Enter remote VNC password" />
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.Row="3" Orientation="Horizontal"
                    HorizontalAlignment="Right" Margin="0,22,0,0">
            <ui:Button Content="Cancel" Click="OnCancelClick"
                       Width="90" Margin="0,0,10,0" />
            <ui:Button Content="Connect" Click="OnConnectClick"
                       Appearance="Primary" Width="90" IsDefault="True" />
        </StackPanel>
    </Grid>
</Window>
